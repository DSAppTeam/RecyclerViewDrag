<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ds.drag.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DSDrag">

        <activity
            android:name=".activity.FolderActivity2"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.AdditionActivity" />
        <activity android:name=".activity.MultiplyActivity" />
        <activity android:name=".activity.NotDragHandlerImplActivity" />
        <activity android:name=".activity.TestDragActivity" />
<!--        <activity android:name=".activity.FolderActivity2" />-->
    </application>

</manifest>